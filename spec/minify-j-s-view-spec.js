'use babel';

import MinifyJSView from '../lib/minify-j-s-view';

describe('MinifyJSView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});

describe('textBoxView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
